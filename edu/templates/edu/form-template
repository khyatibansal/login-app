{% for field in form %}
    <div class="form-group">
            <div>
                    <span class="text-danger la">{{ fiels.errors }}</span>
            </div>

            <label>{{ field.label_tag }}</label>
            <div class="col-sm-10">{{ field }}</div>
    </div>
{% endfor %}